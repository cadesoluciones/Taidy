# Secrets (store securely)
BC_CLIENT_SECRET=
FABRIC_CLIENT_SECRET=

# Optional override for configuration file (defaults to ./config.json)
CONFIG_FILE=./config.json

# Feature toggle (overrides config.json 'enabled' value)
FABRIC_UPLOAD_ENABLED=true
