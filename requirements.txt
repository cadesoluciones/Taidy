# --- Backend (extracción BC / Factorial y subida a Fabric OneLake) ---
requests>=2.32
PyYAML>=6.0
python-dotenv>=1.0
rich>=13.7
tenacity>=8.2
azure-identity>=1.19
azure-storage-file-datalake>=12.18
azure-core>=1.30

# --- Orquestación (webapp/) ---
APScheduler>=3.10

# --- Autenticación local (usuario/contraseña propios) ---
bcrypt>=4.0

# --- Pruebas (webapp/tests/, api/tests/) ---
pytest>=8.0
httpx>=0.27

# --- Lint (dev) ---
# Pinned exactly, not >= : ruff 0.16 silently expanded its default rule set
# (no pyproject.toml/ruff.toml in this repo pins one explicitly) and started
# reporting 760 pre-existing style findings that 0.14.6 never flagged --
# breaking CI with no code change on our side. Bump deliberately, not via drift.
ruff==0.14.6

# --- API backend (api/) ---
fastapi>=0.115
pydantic>=2.6
uvicorn[standard]>=0.30
python-multipart>=0.0.9
